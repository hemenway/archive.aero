# wrangler pulls in `sharp` as an optional dependency whose native binary fails
# to build on this machine (Node 26; prebuilt check falls back to a source build
# that can't complete). wrangler does not need sharp for `deploy`/`dev`, so skip
# dependency lifecycle scripts. `npm run <script>` is unaffected by this setting.
ignore-scripts=true
