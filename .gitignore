# Python
__pycache__/
*.py[cod]
*.egg-info/

# Virtual environments
.venv/
venv/
env/

# IDE / OS
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# Secrets / local env
.env
.env.*
*.secrets
credentials.json

# Separate projects / local tooling
/geotiff2pmtiles
/.claude

# Regenerable caches
bounds_cache.json

# Proprietary catalog — never publish
master_dole_v2.csv

# Catalog-derived / undecided (delete a line to start tracking it)
1georef_toolv10.py
timeline_data.json
worklists/data/
metadata-*.bundle

# Staged for future historical-data feature — 40MB of raw 1988 NOAA DAT files;
# decide what (if anything) to track before committing (README.md is worth tracking)
historical-data/

# Local browser-session recording (1.4 GB) — never commit
archive.aero-recording.json
