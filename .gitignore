# Node / browser tests
node_modules/
test-results/
playwright-report/

# Python
__pycache__/
*.py[cod]
*.egg-info/

# Virtual environments
.venv/
venv/
env/

# IDE / OS
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# Secrets / local env
.env
.env.*
*.secrets
credentials.json

# Separate projects / local tooling
/geotiff2pmtiles
/.claude

# Regenerable caches
bounds_cache.json

# Proprietary catalog — never publish (georef tool leaves .bak/.tmp
# siblings beside it on every save; attic-style .pre_*.bak copies too)
master_dole_v2.csv
master_dole_v2.csv.*

# Catalog-derived / undecided (delete a line to start tracking it)
1georef_toolv10.py
timeline_data.json
worklists/data/
metadata-*.bundle

# Staged for future historical-data feature — 40MB of raw 1988 NOAA DAT files;
# decide what (if anything) to track before committing (README.md is worth tracking)
historical-data/

# Local browser-session recording (1.4 GB) — never commit
archive.aero-recording.json
airfields.json
CLAUDE.md
worklists/09_growth_and_revenue.md
worklists/10_cold_archive.md
worklists/13_sdcard_batch2.md
worklists/14_geotiff2pmtiles_audit_handoff.md
worklists/14_geotiff2pmtiles_upstream_drafts.md
worklists/helper_prompt.md
/.agents
